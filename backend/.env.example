# Text-to-CAD Backend Configuration
# Copy this file to .env and uncomment/modify values as needed

# Development Settings
# DEBUG=true
# LOG_LEVEL=info

# AI/LLM Configuration
# OpenAI configuration (optional; app falls back to rules if missing)
# The backend will only use AI when use_ai=true is sent by the client and a valid key is present
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o
# OPENAI_TIMEOUT_S=20

# Database Configuration (for future use)
# DATABASE_URL=sqlite:///./text_to_cad.db
# DB_ECHO=false

# API Configuration
# API_HOST=0.0.0.0
# API_PORT=8000
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Security (for future use)
# SECRET_KEY=your_secret_key_here
# ACCESS_TOKEN_EXPIRE_MINUTES=30
